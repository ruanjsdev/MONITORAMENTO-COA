{
  "name": "@coa-bot/api",
  "version": "0.1.0",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "@coa-bot/shared": "0.1.0",
    "@coa-bot/validation": "0.1.0",
    "@coa-bot/whatsapp": "0.1.0"
  }
}
