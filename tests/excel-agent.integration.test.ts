import{randomUUID}from"node:crypto";import path from"node:path";import{describe,expect,it}from"vitest";import{executeComCommand}from"../apps/excel-agent/src/com-bridge";import{homologationRoot}from"../apps/excel-agent/src/security";import{ExcelAgentCommand}from"@coa-bot/excel-contracts";
const workbook=path.join(homologationRoot,"Planilha Plantio cana.dev.xlsm");const command=(type:ExcelAgentCommand["type"],payload:ExcelAgentCommand["payload"]={},extra:Partial<ExcelAgentCommand>={}):ExcelAgentCommand=>({commandId:randomUUID(),correlationId:randomUUID(),requestedAt:new Date().toISOString(),requestedBy:"windows-integration-test",type,payload,simulation:true,...extra});
describe.skipIf(process.platform!=="win32")("integração Excel COM Windows somente leitura",()=>{
 it("detecta Excel por COM",async()=>{const result=await executeComCommand(command("CHECK_EXCEL_INSTALLED"));expect(result.success).toBe(true);expect(result.result).toHaveProperty("installed");expect(result.macrosExecuted).toBe(false)});
 it("abre cópia dev e lê célula sem salvar",async()=>{const result=await executeComCommand(command("READ_CELL",{cell:"F8"},{workbook,worksheet:"PLANTIO"}));expect(result.success).toBe(true);expect(result.executed).toBe(true);expect(result.officialWorkbookTouched).toBe(false)});
 it("localiza frota sem escolher correspondência ambígua",async()=>{const result=await executeComCommand(command("FIND_EQUIPMENT",{fleet:"625",fleetColumn:"F",headerRow:7},{workbook,worksheet:"PLANTIO"}));expect(result.success).toBe(true);expect(result.result).toHaveProperty("status")});
});
